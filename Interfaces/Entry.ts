export type Entry = {
    data: {
        Project: string;
        ManagedArea: string;
        LAS: string;
        Operator: string;
        Start: Date;
        End: Date;
    };
    userId: string;
};
